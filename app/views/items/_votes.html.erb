<p class="vote">
<% if current_user && !current_user.item_votable_for?(item) %>
  <%= link_to "🔺いいね!", [:like , item ], method: :patch, class: "btn btn-primary" %>
<% else %>
  <%= link_to "🔻とりけし", [:unlike , item ], method: :patch, class: "btn btn-danger" %>
    you liked♥
<% end %>
</p>
