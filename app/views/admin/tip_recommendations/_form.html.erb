<%= form_for([:admin,@tip_recommendation]) do |f| %>
  <% if @tip_recommendation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tip_recommendation.errors.count, "error") %> prohibited this tip_recommendation from being saved:</h2>

      <ul>
      <% @tip_recommendation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.number_field :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
