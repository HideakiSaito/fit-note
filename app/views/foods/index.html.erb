<p id="notice"><%= notice %></p>

<h1>Listing Foods</h1>

<table class="table table-striped table-hover ">
  <thead>
    <tr>
    </tr>
  </thead>
  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= link_to food.id.to_s + "." + food.name, edit_food_path(food) %></td>
        <td><%= food.diet.name %>
        <table border=1 style="text-align: right; width: 22em;">
          <thead><tr>
              <th>炭水化物:</th>
              <th>脂肪:</th>
              <th>たんぱく質:</th>
              <th> 野菜:</th></tr></thead>
          <tbody><tr>
              <td><%= food.carbohydrate %></td>
              <td><%= food.fat %></td>
              <td><%= food.protein %></td>
              <td><%= food.vegetable %></td>
        </td>
          </tbody></table>
          <br><p><%= food.diet_memo %></p>
          </td>
            </tr>
          <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Food', new_food_path %>
